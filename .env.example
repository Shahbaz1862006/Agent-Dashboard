# Use in-memory mock API (no HTTP). Set to true for mock; false for real backend.
# When true: all API calls use src/services/mock/* (Promises + latency, no network).
# When false: all API calls use src/services/api/* (real HTTP to VITE_API_BASE_URL).
VITE_USE_MOCK_API=true

# API base URL for all dashboard API calls (used only when VITE_USE_MOCK_API=false).
# - Leave empty to use same-origin /api (proxied to mock server in dev).
# - Production: set to your real backend URL (e.g. https://api.yourbackend.com).
VITE_API_BASE_URL=

# Optional: where Vite proxies /api in dev (default http://localhost:3001).
# Only used when VITE_API_BASE_URL is not set (same-origin /api requests).
# VITE_MOCK_API_TARGET=http://localhost:3001
